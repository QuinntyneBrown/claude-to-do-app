# BI1 / B-012 — Todo: update

Implementer / evaluator: `claude@M5`.
Acceptance tests:
- `B012_UpdateTodoTests.Update_todo_persists_new_fields`
- `B012_UpdateTodoTests.Update_todo_does_not_change_status`
- `B012_UpdateTodoTests.Update_other_users_todo_returns_404`

## Per-slice loop trace

1. **Tests first** (commit `e2b7658`). References `PUT /api/todos/{id}`, the `TodoDetail` shape returned, and the rule that `Update` MUST NOT touch `Status` (toggle is its own command).
2. **Implementation:**
   - **Application:**
     - `Todos.UpdateTodo.UpdateTodoCommand(Id, Title, Notes?, DueDate?)`. No `Status` field — by design.
     - `UpdateTodoCommandValidator`: same shape as `CreateTodoCommandValidator` (title 1–200, notes 0–2000, due-date ≥ today).
     - `UpdateTodoCommandHandler`: composite filter `t.Id == id && t.UserId == _currentUser.UserId` so cross-user PUT yields 404 via the same code path; updates only `Title`, `Notes`, `DueDate`; the activity strip and status are untouched. Returns the same `TodoDetail` shape as B-011 so the API surface is consistent.
   - **Api:** `TodosController.Update` (`PUT /api/todos/{id:guid}`). New transport DTO `UpdateTodoRequest` — same shape as `CreateTodoRequest` (no `Status` — same intent).
3. **Build + test:** `dotnet build -warnaserror` 0/0; `dotnet test` 36/36 green (3 new + 33 pre-existing).
4. **Implementation Evaluation Rubric (backend-scoped):**
   - Guidance adherence — ✓ (Clean Arch / MediatR / FluentValidation per command / `IAppDbContext` / no repository or UoW / one-type-per-file / SQL Server).
   - Requirements coverage — ✓ for REQ-TODO-4 write leg (update persists new fields; same 404-on-not-owned isolation as B-011).
   - Radically simple — ✓. No new abstractions; the handler reuses the read-side projection from B-011.
   - No temp code or stubs — ✓.
   - One type per file — ✓.
   - SOLID + CQS shape — ✓.
   - ATDD evidence — ✓ (test commit `e2b7658` precedes implementation).
   - Build and run clean — ✓.

   Zero findings on the rubric pass. Slice done.
