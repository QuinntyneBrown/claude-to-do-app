# BI1 / B-006 — Account: get profile + update display name

Implementer / evaluator: `claude@M5`.
Acceptance tests:
- `B006_AccountProfileTests.Get_me_returns_authenticated_users_profile`
- `B006_AccountProfileTests.Update_display_name_persists_and_returns_new_profile`
- `B006_AccountProfileTests.Update_display_name_rejects_blank_or_too_long`

## Per-slice loop trace

1. **Tests first** (commit `adfd1b3`). References `MyProfile`, `/api/account/me`, `/api/account/display-name` — none of which existed pre-slice.
2. **Implementation:**
   - **Application:**
     - `Account.MyProfile` shared response shape (`Email`, `DisplayName`, `PendingEmail?`). `PendingEmail` is always `null` until B-007 lands.
     - `Account.GetMyProfile.GetMyProfileQuery` + handler — scopes by `ICurrentUserService.UserId`; throws `NotFoundException` (mapped to 404) if the user is missing.
     - `Account.UpdateDisplayName.UpdateDisplayNameCommand` + validator + handler. Validator: `NotEmpty + MaximumLength(100)`. Handler trims, persists, returns the updated `MyProfile`.
   - **Api:** new `AccountController` decorated `[Authorize(Roles = "User")]` at the controller level. `GET /api/account/me` → `GetMyProfileQuery`. `PUT /api/account/display-name` → `UpdateDisplayNameCommand`. New transport DTO `UpdateDisplayNameRequest` (one type per file).
3. **Build + test:** `dotnet build -warnaserror` 0/0; `dotnet test` 19/19 green (3 new + 16 pre-existing).
4. **Implementation Evaluation Rubric (backend-scoped):**
   - Guidance adherence — ✓ (Clean Arch / MediatR / `IAppDbContext` / no repository or UoW / FluentValidation per command / one-type-per-file / SQL Server).
   - Requirements coverage — ✓ for REQ-ACCT-1 (view profile) and REQ-ACCT-2 AC1 (update display name persists, validator rejects out-of-range input).
   - Radically simple — ✓. No new abstractions; the slice consumes `IAppDbContext` and `ICurrentUserService` only.
   - No temp code or stubs — ✓. The `PendingEmail = null` placeholder is the deliberate B-007 hand-off and is documented in the slice's plan entry.
   - One type per file — ✓.
   - SOLID + CQS shape — ✓ (controller delegates to MediatR; handlers per-user-scoped via `ICurrentUserService`).
   - ATDD evidence — ✓ (test commit `adfd1b3` precedes implementation).
   - Build and run clean — ✓.

   Zero findings on the rubric pass. Slice done.
