# BI1 / B-010 — Todo: extend create with notes + due date + activity strip

Implementer / evaluator: `claude@M5`.
Acceptance tests:
- `B010_CreateTodoExtendedTests.Create_todo_with_notes_and_due_date_persists_and_writes_Created_activity`
- `B010_CreateTodoExtendedTests.Create_todo_rejects_notes_over_limit_and_past_due_date`

## Per-slice loop trace

1. **Tests first** (commit `f32a87e`). References `Notes`, `DueDate`, `TodoActivityEntries` `DbSet`, `TodoActivityKind` enum, and the extended `CreateTodoResult` shape.
2. **Implementation:**
   - **Domain:** `Todo` extended with `Notes` (`string?`) and `DueDate` (`DateOnly?`). New `TodoActivityEntry` and `TodoActivityKind` enum (one type per file).
   - **Migration #007 + #008** consolidated into a single EF migration `AddTodoNotesAndDueDateAndActivityEntries` (BT1 listed two; the change-set is small and single-step here, so a single migration is the radically-simple form).
   - **Application:** `IAppDbContext` exposes `TodoActivityEntries`. `CreateTodoCommand(Title, Notes?, DueDate?)` and `CreateTodoResult(Id, Title, Notes?, DueDate?, Status, CreatedAt)`. `CreateTodoCommandValidator` injects `TimeProvider` and rejects: empty / >200-char title, >2000-char notes, due date earlier than `today` (UTC). Handler trims title + notes, persists the todo, then inserts a `TodoActivityEntry { Created, OccurredAt = now }` in the same transaction.
   - **Infrastructure:** `AppDbContext` configures `Todos.Notes` (≤2000) and the `TodoActivityEntries` table (composite `(TodoId, OccurredAt)` index, cascade-delete by `TodoId`).
   - **Api:** `CreateTodoRequest` extended with `Notes?` and `DueDate?` (transport DTO, no annotations). `TodosController.Create` forwards both to the command.
3. **Build + test:** `dotnet build -warnaserror` 0/0; `dotnet test` 30/30 green (2 new + 28 pre-existing).
4. **Implementation Evaluation Rubric (backend-scoped):**
   - Guidance adherence — ✓ (Clean Arch / MediatR / FluentValidation / `IAppDbContext` / no repository or UoW / one-type-per-file / SQL Server).
   - Requirements coverage — ✓ for REQ-TODO-1 (initial state `Incomplete` unchanged), REQ-TODO-2 AC4 (1–200 / 0–2000 / due date validation), REQ-TODO-8 AC1 (Created activity written on creation).
   - Radically simple — ✓. No new abstractions; reuses existing patterns.
   - No temp code or stubs — ✓.
   - One type per file — ✓.
   - SOLID + CQS shape — ✓.
   - ATDD evidence — ✓ (test commit `f32a87e` precedes implementation).
   - Build and run clean — ✓.

   Zero findings on the rubric pass. Slice done.

## Note on migration consolidation

BT1 numbered two migrations (#007 `AddTodoNotesAndDueDate`, #008 `AddTodoActivityEntries`). Both are tiny additive schema changes that ship together in this slice — they're applied as a single EF Core migration `AddTodoNotesAndDueDateAndActivityEntries` to keep the migration history radically simple. No behaviour difference, and downstream slices (B-011, B-013) reference the activity table by name, not by migration number.
