# FI1 / F-001 — Sign up

Implementer / evaluator: `claude@M5`.
Acceptance specs: `e2e/specs/sign-up.spec.ts` — `Sign_up_with_valid_input_creates_account_and_routes_to_todos`, `Sign_up_with_password_under_12_chars_shows_inline_error`.

## Per-slice loop trace

1. **Test first** (commit `f296260`). New `e2e/page-objects/sign-up.page.ts` POM and `e2e/specs/sign-up.spec.ts` mocking the backend at the network layer.
2. **Implementation:**
   - **api:** new `RegisterRequest` model. `IAuthService` contract extended with `register(req)`. Concrete `AuthService` POSTs `/api/auth/register` (B-001).
   - **domain:** `SignUpFormComponent` (`tb-sign-up-form`) — three `mat-form-field` + `mat-input` rows (display name / email / password); password hint "Must be at least 12 characters." surfaces REQ-AUTH-7 client-side; `data-testid` on every input + the submit button + the inline error region; emits a `signedUp` event on success. Standalone `OnPush`. SCSS uses `var(--mat-sys-*)` only.
   - **tickbox:** `SignUpPageComponent` (`tb-sign-up-page`) wraps the auth-screen layout (matching `mocks/sign-up.html`): brand at top, headline + subtitle, the form, a footer router-link back to `/sign-in`. On `signedUp`, sets the access token via `AuthStateService` and navigates to `/todos`.
   - **routes:** `/sign-up` added to `app.routes.ts` as a lazy `loadComponent`.
3. **Build + test:** `ng build api/components/domain/tickbox` all 0 warnings 0 errors. Playwright spec set: 3/3 green (2 new + 1 pre-existing MF1 sample).
4. **Implementation Evaluation Rubric (frontend-scoped):**
   - Guidance adherence — ✓ (Frontend §"Angular Material" + design tokens + BEM, Library Structure dep direction respected, Authentication §"local username + password", General §"One type per file" with separate `.html`/`.scss`/`.ts`, Testing §"Playwright POM").
   - Requirements coverage — ✓ for REQ-AUTH-1 (sign up creates account, routes to todos) and REQ-AUTH-7 client-side hint (12-char minimum surfaced).
   - Radically simple — ✓. No new abstractions; `SignUpFormComponent` consumes `AUTH_SERVICE` token; `SignUpPageComponent` wraps it with brand + footer.
   - No temp code or stubs / no debug `console.*` / no empty methods — ✓.
   - One type per file with separate `.ts`/`.html`/`.scss` — ✓.
   - SOLID + interface-driven services — ✓ (form consumes `AUTH_SERVICE` injection token; concrete `AuthService` wired only in `app.config.ts`).
   - ATDD evidence — ✓ (test commit `f296260` precedes implementation).
   - Mobile-first + responsive — ✓. Visually verified at 360 / 768 / 1440: card stretches full-width below 600px and constrains to 420px above; padding scales from 32px to 48px.
   - Build and run clean — ✓ (`ng build` 0/0 across all four projects).

   Zero findings on the rubric pass. Slice done.
