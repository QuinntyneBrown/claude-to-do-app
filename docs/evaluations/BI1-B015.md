# BI1 / B-015 — Todo: list ordering rule

Implementer / evaluator: `claude@M5`.
Acceptance test: `B015_TodoListOrderingTests.Get_todos_orders_by_due_date_ascending_nulls_last_then_created_at_descending`.

## Per-slice loop trace

1. **Tests first** (commit `2f1dab0`). Creates 5 todos in a known relative `CreatedAt` order with mixed-presence `DueDate`s and asserts the response order honours the rule.
2. **Implementation:**
   - **Application:**
     - `TodoListItem` extended with `Notes`, `DueDate` (the entity columns added in B-010 are now surfaced on the read DTO so the frontend list can render due-date affordances and tooltips).
     - `GetTodosQueryHandler` ordering rewritten to: `OrderBy(t => t.DueDate == null) ThenBy(t => t.DueDate) ThenByDescending(t => t.CreatedAt)`. The first projection (a boolean expression) places nulls last because `false` (`0`) sorts before `true` (`1`); the second is the asc-by-date rule among non-nulls; the third is the deterministic tiebreak. Same query under any future filter chip — the backend always returns the full owned set; the frontend filters.
3. **Build + test:** `dotnet build -warnaserror` 0/0; `dotnet test` 31/31 green (1 new + 30 pre-existing).
4. **Implementation Evaluation Rubric (backend-scoped):**
   - Guidance adherence — ✓ (Clean Arch / MediatR / `IAppDbContext` / no repository or UoW / one-type-per-file / SQL Server).
   - Requirements coverage — ✓ for REQ-TODO-3 AC4 (server-side ordering: `due_date` ascending nulls-last, then `created_at` descending; deterministic).
   - Radically simple — ✓. One LINQ chain change; the read DTO grew two fields that already exist on the entity.
   - No temp code or stubs — ✓.
   - One type per file — ✓.
   - SOLID + CQS shape — ✓.
   - ATDD evidence — ✓ (test commit `2f1dab0` precedes implementation).
   - Build and run clean — ✓.

   Zero findings on the rubric pass. Slice done.
