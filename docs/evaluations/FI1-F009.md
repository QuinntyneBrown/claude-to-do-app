# FI1 / F-009 — Delete account

Implementer / evaluator: `claude@M5`.
Acceptance specs: `e2e/specs/profile-delete-account.spec.ts` — `Delete_account_confirm_signs_out_and_routes_to_sign_in`, `Delete_account_cancel_closes_dialog_without_deleting`.

## Per-slice loop trace

1. **Tests first** (commit `94b3bf2`). New `e2e/specs/profile-delete-account.spec.ts` (2 cases). Reuses the existing `ProfilePage` POM and the `confirm-dialog-confirm` / `confirm-dialog-cancel` testIds already exposed by the F-006 `ConfirmDialogComponent`.
2. **Implementation:**
   - **api:** `IAccountService` extended with `deleteMyAccount(): Observable<void>`. Concrete `AccountService` issues DELETE `/api/account` (B-009 endpoint).
   - **domain:** `DeleteAccountSectionComponent` (`tb-delete-account-section`) — danger-zone block with a "Delete account" `mat-stroked-button color="warn"`. Click opens `ConfirmDialogComponent` (from F-006) with the destructive variant: confirm label "Delete account", message warns about permanent removal of every owned to-do. On confirm, calls `IAccountService.deleteMyAccount` and emits `(deleted)`. On cancel (or dismiss), no API call.
   - **tickbox:** `ProfilePageComponent` adds the section under a "Danger zone" heading and routes the `(deleted)` event through `onAccountDeleted`, which reuses `completeSignOut` (clears `AuthStateService` access token + routes to `/sign-in`). Sign-out and account-delete share the same terminal flow — one private method, no duplication.
3. **Build + test:** `ng build api/components/domain/tickbox` all 0 warnings 0 errors. Playwright spec set: 35/35 green (2 new + 33 from prior slices).
4. **Implementation Evaluation Rubric (frontend-scoped):**
   - Guidance adherence — ✓ (Material 3 button + dialog primitives only, design tokens via `var(--mat-sys-*)`, BEM naming, standalone OnPush, injection-token consumption of `IAccountService`).
   - Library boundaries — ✓ (`DeleteAccountSectionComponent` lives in `domain` because it injects an api service; `ConfirmDialogComponent` reused from `components` unchanged).
   - Requirements coverage — ✓ for REQ-ACCT-4 (destructive confirm dialog precedes any API call; on success the user is signed out and routed to `/sign-in`; on cancel nothing happens).
   - Radically simple — ✓. No new dialog, no new dialog-result type, no new "danger zone" component in `components` — the per-page section component is the smallest thing that could work, and it reuses everything F-006 already shipped.
   - One type per file with separate `.ts`/`.html`/`.scss` — ✓ (1 new component in `domain`).
   - SOLID + interface-driven services — ✓ (one-method extension on `IAccountService`).
   - ATDD evidence — ✓ (test commit `94b3bf2` precedes implementation commit `d005ea3`).
   - Mobile-first + responsive — ✓. Section stacks vertically; the destructive button stays a 48dp+ tap target at 360px width. The confirm dialog inherits Material 3's adaptive width.
   - Build and run clean — ✓.

   Zero findings on the rubric pass. Slice done.
