# FI1 / F-002 — Sign-in extension + token refresh interceptor + OIDC button + ErrorBanner seed

Implementer / evaluator: `claude@M5`.
Acceptance specs: `e2e/specs/sign-in.spec.ts` — `Sign_in_with_valid_creds_routes_to_todos`, `Sign_in_with_wrong_password_shows_inline_error`, `Sign_in_hides_oidc_button_when_disabled`, `Sign_in_shows_oidc_button_when_enabled`, `Refresh_interceptor_silently_renews_session_on_401`.

## Per-slice loop trace

1. **Tests first** (commit `2fd4306`). Replaces the MF1 `sample-flow.spec.ts` with `sign-in.spec.ts` (5 cases). Adds `oidcButton` and `forgotPasswordLink` locators to `SignInPage` page object.
2. **Implementation:**
   - **api:** `IAuthService.refreshAccessToken(): Observable<SignInResponse>` added to contract; `AuthService.refreshAccessToken` POSTs `/api/auth/refresh` with `withCredentials: true` so the HttpOnly refresh-token cookie attaches.
   - **components:** new `ErrorBannerComponent` (`tb-error-banner`) — `@Input() message`, `@Input() testId`, renders `var(--mat-sys-error-container)`-shaped block with `data-testid` only when explicitly supplied. Standalone OnPush. BEM CSS.
   - **domain:** new `OidcSignInButtonComponent` (`tb-oidc-sign-in-button`) — `mat-stroked-button` with key icon, hidden when `window.__TICKBOX_OIDC_ENABLED__ !== true`. Wiring of the click action lands in F-004.
   - **tickbox:** new `tokenRefreshInterceptor` — on 401 from any non-`/api/auth` endpoint, calls `IAuthService.refreshAccessToken`, replays the original request with the new bearer; on hard 401 (refresh itself failed), signs out via `AuthStateService` and routes to `/sign-in`. Wired in `app.config.ts` after the existing `authInterceptor`.
   - **tickbox:** `SignInComponent` extended — composes the new `ErrorBannerComponent` (replaces the local error `<p>`), the `OidcSignInButtonComponent`, a "Forgot password?" router-link to `/password-reset/request`, and a footer router-link to `/sign-up`. The MVP's `Sign_in_shows_oidc_button_when_enabled` test relies on `addInitScript` setting `window.__TICKBOX_OIDC_ENABLED__ = true`, so the component's getter reads the flag at render time.
3. **Build + test:** `ng build api/components/domain/tickbox` all 0 warnings 0 errors. Playwright spec set: 7/7 green (5 new + 2 from F-001).
4. **Implementation Evaluation Rubric (frontend-scoped):**
   - Guidance adherence — ✓ (Library Structure dep direction respected — `ErrorBannerComponent` lives in `components` with no `api` import; `OidcSignInButtonComponent` lives in `domain`, no api import either since the wiring lands in F-004; refresh interceptor lives in `tickbox` per the auth-glue placement).
   - Requirements coverage — ✓ for REQ-AUTH-2 (sign-in with bad creds shows inline error), REQ-AUTH-3 AC2 (OIDC button hidden when env-disabled), REQ-NFR-6 (refresh-token cookie via `withCredentials: true`; access JWT in `sessionStorage` unchanged from MF1).
   - Radically simple — ✓. The OIDC button is wireless (the click action is wired in F-004 — its test today only verifies presence + visibility); this is intentional staging. No `console.*`, no TODOs, no hard-coded fakes.
   - One type per file with separate `.ts`/`.html`/`.scss` — ✓.
   - SOLID + interface-driven services — ✓ (`tokenRefreshInterceptor` consumes `AUTH_SERVICE` injection token; concrete `AuthService` only wired in `app.config.ts`).
   - ATDD evidence — ✓ (test commit `2fd4306` precedes implementation).
   - Mobile-first + responsive — ✓ at 360 / 768 / 1440 (the sign-in card constrains to 420px max above 600px).
   - Build and run clean — ✓ (`ng build` 0/0 across all four projects).

   Zero findings on the rubric pass. Slice done.
