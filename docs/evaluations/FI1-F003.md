# FI1 / F-003 — Password reset (request + complete)

Implementer / evaluator: `claude@M5`.
Acceptance specs: `e2e/specs/password-reset.spec.ts` — `Request_password_reset_shows_inline_confirmation_for_any_email`, `Complete_password_reset_with_valid_token_signs_in`, `Complete_password_reset_with_expired_token_shows_inline_error`.

## Per-slice loop trace

1. **Tests first** (commit `605243b`). New page objects `password-reset-request.page.ts`, `password-reset-complete.page.ts` and `password-reset.spec.ts` (3 cases).
2. **Implementation:**
   - **api:** new `PasswordResetRequest` and `CompletePasswordResetRequest` types on the contract; `IAuthService.requestPasswordReset(req)` and `completePasswordReset(req)` added; concrete `AuthService` methods POST B-004's endpoints.
   - **domain:** `PasswordResetRequestFormComponent` (`tb-password-reset-request-form`) — single email field + submit; renders an inline confirmation regardless of outcome (no enumeration, mirrors backend behaviour). `PasswordResetCompleteFormComponent` (`tb-password-reset-complete-form`) — required `@Input() token`, new-password field with policy hint, inline error on failure, emits `completed` event with the `SignInResponse` on success.
   - **tickbox:** `RequestPasswordResetPageComponent` (`tb-request-password-reset-page`) and `CompletePasswordResetPageComponent` (`tb-complete-password-reset-page`) — both wrap a shared auth-screen layout (the SCSS file `password-reset-page.component.scss` is reused via `styleUrl`). Complete page reads `token` from `ActivatedRoute.queryParamMap`; on `completed` sets the access token via `AuthStateService` and navigates to `/todos`.
   - **routes:** `/password-reset/request` and `/password-reset/complete` added as lazy `loadComponent`s.
3. **Build + test:** `ng build api/components/domain/tickbox` all 0 warnings 0 errors. Playwright spec set: 10/10 green (3 new + 7 from prior slices).
4. **Implementation Evaluation Rubric (frontend-scoped):**
   - Guidance adherence — ✓ (Library Structure dep direction respected; both forms in `domain` consume `AUTH_SERVICE` injection token; pages in `tickbox` only).
   - Requirements coverage — ✓ for REQ-AUTH-4 AC1 (request returns 202 for any email and shows inline confirmation), AC2 (valid token signs the user in), AC3 (expired token surfaces inline error).
   - Radically simple — ✓. No new abstractions; the two forms reuse the auth-screen layout SCSS via `styleUrl` so the visual treatment matches the request mock without copy-pasting style rules.
   - One type per file with separate `.ts`/`.html`/`.scss` — ✓.
   - SOLID + interface-driven services — ✓ (forms consume `AUTH_SERVICE` injection token).
   - ATDD evidence — ✓ (test commit `605243b` precedes implementation).
   - Mobile-first + responsive — ✓ at 360 / 768 / 1440 (cards constrain to 420px max above 600px; padding scales 32→48px).
   - Build and run clean — ✓ (`ng build` 0/0 across all four projects).

   Zero findings on the rubric pass. Slice done.
