# BI1 / B-011 — Todo: get by id (with activity)

Implementer / evaluator: `claude@M5`.
Acceptance tests:
- `B011_GetTodoByIdTests.Get_todo_by_id_returns_full_detail_including_activity`
- `B011_GetTodoByIdTests.Get_todo_owned_by_other_user_returns_404`

## Per-slice loop trace

1. **Tests first** (commit `fde6498`). Tests reference the new `Tickbox.Application.Todos.GetTodoById.TodoDetail` shape and the `GET /api/todos/{id}` endpoint.
2. **Implementation:**
   - **Application:**
     - `Todos.GetTodoById.GetTodoByIdQuery(Guid Id)`.
     - `Todos.GetTodoById.TodoActivityItem(Kind, OccurredAt)` — read-side projection of `TodoActivityEntry` (entity stays in Domain).
     - `Todos.GetTodoById.TodoDetail(Id, Title, Notes?, DueDate?, Status, CreatedAt, CompletedAt?, Activity[])`.
     - `Todos.GetTodoById.GetTodoByIdQueryHandler` — single composite filter `t.Id == id && t.UserId == _currentUser.UserId` so "not found" and "not owned by caller" produce the same `NotFoundException` (mapped by the existing middleware to 404). REQ-NFR-5 isolation + REQ-TODO-4 no-enumeration.
     - Activity is loaded separately, ordered by `OccurredAt` ascending, projected into `TodoActivityItem[]`.
   - **Api:** `TodosController.GetById` (`GET /api/todos/{id:guid}`). `Create` now uses `CreatedAtAction(nameof(GetById), new { id = result.Id }, result)` so `Location` headers point at the new endpoint.
3. **Build + test:** `dotnet build -warnaserror` 0/0; `dotnet test` 33/33 green (2 new + 31 pre-existing).
4. **Implementation Evaluation Rubric (backend-scoped):**
   - Guidance adherence — ✓ (Clean Arch / MediatR / `IAppDbContext` / no repository or UoW / one-type-per-file / SQL Server).
   - Requirements coverage — ✓ for REQ-TODO-4 (read leg — return full detail), REQ-TODO-8 (read leg — Activity[] sourced from `TodoActivityEntries`), REQ-NFR-5 (per-user isolation; same-code-path 404).
   - Radically simple — ✓. No new abstractions; reads through `IAppDbContext`.
   - No temp code or stubs — ✓.
   - One type per file — ✓.
   - SOLID + CQS shape — ✓.
   - ATDD evidence — ✓ (test commit `fde6498` precedes implementation).
   - Build and run clean — ✓.

   Zero findings on the rubric pass. Slice done.
