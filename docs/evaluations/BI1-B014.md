# BI1 / B-014 — Todo: delete

Implementer / evaluator: `claude@M5`.
Acceptance tests:
- `B014_DeleteTodoTests.Delete_todo_removes_row_and_cascades_activity`
- `B014_DeleteTodoTests.Delete_other_users_todo_returns_404`

## Per-slice loop trace

1. **Tests first** (commit `28d31d0`). Tests reference `DELETE /api/todos/{id}` and the cascade behaviour against `TodoActivityEntries` plus the per-user 404 isolation rule.
2. **Implementation:**
   - **Application:** `Todos.DeleteTodo.DeleteTodoCommand(Id)` + handler. Composite filter `t.Id == id && t.UserId == _currentUser.UserId` so cross-user delete yields 404 via the same code path. Explicitly removes the related `TodoActivityEntries` rows then the `Todo`, mirroring the relational cascade so the InMemory test provider matches SQL Server behaviour.
   - **Api:** `TodosController.Delete` (`DELETE /api/todos/{id:guid}`). Returns 204.
3. **Build + test:** `dotnet build -warnaserror` 0/0; `dotnet test` 41/41 green (2 new + 39 pre-existing).
4. **Implementation Evaluation Rubric (backend-scoped):**
   - Guidance adherence — ✓ (Clean Arch / MediatR / `IAppDbContext` / no repository or UoW / one-type-per-file / SQL Server).
   - Requirements coverage — ✓ for REQ-TODO-6 (hard delete, no undo). Per-user isolation via `ICurrentUserService` (REQ-NFR-5).
   - Radically simple — ✓. No new abstractions; the explicit activity removal is the same pattern used by B-009.
   - No temp code or stubs — ✓.
   - One type per file — ✓.
   - SOLID + CQS shape — ✓.
   - ATDD evidence — ✓ (test commit `28d31d0` precedes implementation).
   - Build and run clean — ✓.

   Zero findings on the rubric pass. Slice done.

## BI1 status after this slice

All 15 BI1 slices (B-001 → B-015) are now complete with passing acceptance tests. Aggregate: 41 tests across 15 slices, 0 failures, 0 build warnings, 0 build errors.
