# BI1 / B-002 — Audit log + sign-in lockout + `IRequestContext`

Implementer / evaluator: `claude@M5`.
Acceptance tests: `B002_AuditAndLockoutTests.Sign_in_locks_out_after_five_failures_in_window`, `B002_AuditAndLockoutTests.Sign_in_with_null_password_hash_account_returns_401_generic`.

## Per-slice loop trace

1. **Tests first** (commit `17ce2fa`). Compile-red because they reference types this slice introduces.
2. **Implementation:**
   - **Domain:** `SignInAttempt`, `SecurityAuditEvent`, `SecurityAuditKind` enum (one type per file).
   - **Application:** `IRequestContext` interface (`RemoteIp`, `UserAgent`); `IAppDbContext` exposes `SignInAttempts` + `SecurityAuditEvents` `DbSet`s. `SignInUserCommandHandler` now: counts failed `SignInAttempt`s in the prior 15 minutes, locks out at 5+ with a `SignInLocked` audit, writes a `SignInFailed` audit on bad credentials and on null-password-hash, records every attempt (success or failure) in `SignInAttempts`. The handler depends on `IAppDbContext`, `IPasswordHasher`, `IJwtTokenService`, `IRequestContext`, `TimeProvider` — all interface-driven.
   - **Infrastructure:** `AppDbContext` configures the two new tables (`Email,OccurredAt` index for the lockout query; `UserId,OccurredAt` index for audit reads). `SecurityAuditKind` persisted as `int` via `HasConversion<int>()`.
   - **Migration #003 `AddSignInAttemptsAndAuditEvents`** (created via `dotnet ef migrations add`).
   - **Api:** `RequestContext : IRequestContext` reads `IHttpContextAccessor.HttpContext.Connection.RemoteIpAddress` and the `User-Agent` header. Registered scoped in `Program.cs`.
3. **Build + test:** `dotnet build -warnaserror` 0/0; `dotnet test` 6/6 green.
4. **Implementation Evaluation Rubric (backend-scoped):**
   - Guidance adherence — ✓ (Clean Arch, MediatR untouched, `IAppDbContext` extended not abstracted further, no repository / UoW, FluentValidation untouched, JWT validation untouched, MS SQL Server, one-type-per-file).
   - Requirements coverage in entirety — ✓ for REQ-AUTH-2 AC3 (5/15-min lockout + audit) and REQ-NFR-4 (audit table + sign-in events).
   - Radically simple — ✓ (one new interface `IRequestContext` justified by lockout + audit IP write, no other speculation).
   - No temp code or stubs — ✓.
   - One type per file — ✓ (every new entity / enum / interface / impl in its own `.cs`).
   - SOLID + CQS shape — ✓ (handler depends on abstractions; `RequestContext` lives in `Tickbox.Api` so Application stays HttpContext-free).
   - ATDD evidence — ✓ (test commit `17ce2fa` precedes the implementation commit).
   - Build and run clean — ✓.

   Zero findings on the rubric pass. Slice done.
